package com.delivery.sopo.enums

enum class SnackBarEnum
{
    /**
     * 일반 에러 - 유효성 체크
     * 택배 등록 - 버튼
     * 네트워크 에러
     */

    COMMON,
    UPDATE,
    CONFIRM_DELETE,
    CONNECT_NETWORK,
    DISCONNECT_NETWORK,
    ERROR
}